<div class="container">
  <div class="row">
    <!-- ユーザーの詳細 -->
    <%= render 'side', user: @user %>
    <!-- ユーザーの投稿一覧 -->
    <div class="col-md-8 offset-md-1 d-flex flex-column align-items-center">
      <%= render 'posts/posts_index', posts: @posts %>
    </div>
  </div>
</div>